<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: 2025 The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<!-- These resources are around just to allow their values to be customized
     for different hardware and product builds. -->
<resources>
    <!-- Doze: does this device support STATE_DOZE?
         The display is dozing in a low power state; it is still on but is
         optimized for showing system-provided content while the device is
         non-interactive. -->
    <bool name="doze_display_state_supported">true</bool>

    <!-- Doze: does this device support STATE_DOZE_SUSPEND?
        Display state: The display is dozing in a suspended low power state;
        it is still on but the CPU is not updating it. This may be used in
        one of two ways: to show static system-provided content while the
        device is non-interactive, or to allow a "Sidekick" compute
        resource to update the display. For this reason, the CPU must not
        control the display in this mode. -->
    <bool name="doze_suspend_display_state_supported">true</bool>

    <!-- Should we vibrate on an icon animation of the shelf. This should only be active if the
          vibrator is capable of subtle vibrations -->
    <bool name="config_vibrateOnIconAnimation">true</bool>

    <!-- Whether to show a warning notification when device's skin temperature is high. -->
    <integer name="config_showTemperatureWarning">1</integer>

    <!-- Whether to show a alarm dialog when device's usb port is overheating. -->
    <integer name="config_showUsbPortAlarm">1</integer>

    <!-- Threshold in micro watts above which a charger is rated as "fast"; 25W  -->
    <integer name="config_chargingFastThreshold">25000000</integer>

    <!-- Threshold in micro watts above which a charger is rated as "fast"; 45W  -->
    <integer name="config_chargingFastThreshold_v2">45000000</integer>

    <!-- Preferred max refresh rate at keyguard, if supported by the display. -->
    <integer name="config_keyguardMaxRefreshRate">60</integer>
</resources>